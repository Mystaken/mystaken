import { extendTheme } from "@chakra-ui/react";
export const testOutput = 1;
export const theme = extendTheme({
  fonts: {
    heading: `'Open Sans', sans-serif`,
    body: `'Raleway', sans-serif`,
  },
});
